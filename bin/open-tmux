#!/usr/bin/env bash

# launch tmux with options & keep shell open ok
tmuxs() {
  tmux new-session -n cli \;\
    new-window -n dev \;\
    split-window -p 20 \;\
    set-window-option synchronize-panes \;\
    select-window -t 1 \;
  $SHELL
}

export -f tmuxs

gnome-terminal -- bash -c 'tmuxs'
