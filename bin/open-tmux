#!/usr/bin/env bash

# launch tmux with options & keep shell open ok
tmuxs() {
  tmux new-session \; split-window -v \; resize-pane -y 9\; rename-window tmux \; attach
  $SHELL
}

export -f tmuxs

gnome-terminal -- bash -c 'tmuxs'
