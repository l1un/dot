#!/usr/bin/env bash

# launch default or optionally named gnome-terminal right lower corner with tmux
tmux_gnome_terminal() {
  # use arg if passed, if not set default profile
  local profile=${1:-default}
  gnome-terminal --profile="$profile" --geometry=85x25-0-0 -- tmux
}

tmux_gnome_terminal "$1"
